services:
  - type: web
    name: webapp
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      # DATABASE_URL is manually configured in Render dashboard
      # Uncomment below if you want to use render.yaml database reference
      # - key: DATABASE_URL
      #   fromDatabase:
      #     name: webapp-db
      #     property: connectionString

# Uncomment below if you want Render to create database automatically
# databases:
#   - name: webapp-db
#     plan: free
#     databaseName: webapp
#     user: webapp
